{
  "name": "win-dir",
  "version": "1.0.0",
  "description": "A drop-in replacement for dir on windows when not using the shell",
  "bin": "bin/win-dir",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "argv": "0.0.2"
  }
}
